# Environment Variable Template.
# This is meant to be processed by deployment `add-secrets.js` script and appended to the app.yaml 
# file before deployment
env_variables:
  SSH_PORT: <% SSH_PORT %>
  SSH_HOST: <% SSH_HOST %>
  SSH_USERNAME: <% SSH_USERNAME %>
  SSH_PASSWORD: <% SSH_PASSWORD %>
